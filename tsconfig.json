{
    "compilerOptions": {
      "rootDir": "src", // Ensures the output directory folder structure is the same as the src folder structure.
      "outDir": "dist",
      "target": "es2017",
      "sourceMap": true,
      "lib": [
        "webworker", // Needed so the PostMessage in the worker can have only one arg (regular post message has 2).
        "es6",
        "scripthost"
      ]
    }
  }